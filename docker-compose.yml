services:
  db:
    image: postgres:15
    container_name: lern_postgres_db
    environment:
      POSTGRES_DB: lern_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
